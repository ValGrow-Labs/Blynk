# Copy to android/secrets.properties (git-ignored) and fill in a Maps SDK for Android key.
# Alternatively set the MAPS_API_KEY environment variable or add it to android/local.properties.
MAPS_API_KEY=
