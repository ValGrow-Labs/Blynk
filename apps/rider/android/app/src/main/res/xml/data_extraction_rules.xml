<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (API 31+) controls cloud backup and device-to-device transfer
  through these rules; on some devices android:allowBackup="false" alone only
  stops the cloud backup. The rider's session (a 30-day refresh token) lives
  in WebView localStorage, so nothing may leave the device by either route.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
